@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Albums
@{
    <button class="btn btn-primary my-4" onclick="@("window.location.href='" + @Url.Action("ChooseSong","Album", new { albumId = Model.Id}) + "'");">Add To Album</button>
    @foreach (var i in ViewBag.Songs)
    {
        <div class="d-flex align-items-center justify-content-between bg-gray ">
            <div class="d-flex align-items-center justify-content-between w-100">
                <span class="fw-bold h4 ">@i.Artists - @i.SongName</span>
                <div class="d-flex align-items-center justify-content-between bg-gray ">
                    <audio controls class="align-self-end">
                        <source src="@i.Path" type="audio/mpeg">
                    </audio>
                    <button class="btn btn-primary" onclick="@("window.location.href='" + @Url.Action("DeleteFromAlbum","Album", new { songId = i.SongId, albumId = Model.Id}) + "'");">Delete From Album</button>
                </div>
               
            </div>
            
        </div>
    }
}
